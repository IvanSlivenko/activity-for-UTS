      Если РольДоступна("ПолныеПрава") Тогда
      
      если не отказ тогда
		если СтатусЗаказа= Перечисления.СтатусыЗаказов.Відвантаженно тогда
			СтатусЗаказа =неопределено;
			Если Модифицированность() Тогда
    			Записать();
 			КонецЕсли;

			Если ЗначениеЗаполнено(Сделка) и (ТипЗнч(Сделка) = Тип("ДокументСсылка.ЗаказПокупателя")) тогда
				измЗаказ = Сделка.ПолучитьОбъект();
				измЗаказ.СтатусЗаказа = СтатусЗаказа;
				измЗаказ.Записать(); 
			КонецЕСли;
        КонецЕСли;
	конецЕсли;
	
	ОбщегоНазначения.УдалитьДвиженияРегистратора(ЭтотОбъект, Отказ);


     Иначе
	Если Дата<ТекущаяДата()  Тогда
		Отказ = Истина;
		Иначе
КонецЕсли;
	КонецЕсли;
	
	
	
	резерв  (строка 6649);
	
	если не отказ тогда
		если СтатусЗаказа= Перечисления.СтатусыЗаказов.Відвантаженно тогда
			СтатусЗаказа =неопределено;
			Если Модифицированность() Тогда
    			Записать();
 			КонецЕсли;

			Если ЗначениеЗаполнено(Сделка) и (ТипЗнч(Сделка) = Тип("ДокументСсылка.ЗаказПокупателя")) тогда
				измЗаказ = Сделка.ПолучитьОбъект();
				измЗаказ.СтатусЗаказа = СтатусЗаказа;
				измЗаказ.Записать(); 
			КонецЕСли;
        КонецЕСли;
	конецЕсли;
	
	ОбщегоНазначения.УдалитьДвиженияРегистратора(ЭтотОбъект, Отказ


Возврат товаров от покупателя  стр. 4158

РТУ стр. 6004

текущие - закометировать

новый вставить;




